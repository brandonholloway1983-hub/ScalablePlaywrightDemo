{
  "name": "scalable-playwright-demo",
  "version": "1.0.0",
  "description": "Enterprise-focused Playwright framework with AI-powered failure analysis, two-stage CI, and storageState auth — built against OrangeHRM as an analog for complex enterprise applications",
  "author": "Brandon Holloway",
  "type": "module",
  "scripts": {
    "test:smoke": "playwright test --project=chromium-smoke",
    "test:regression": "playwright test --project=chromium-regression",
    "test:all": "playwright test --project=chromium-smoke --project=chromium-regression",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "report:html": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "allure-playwright": "^3.0.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0"
  }
}